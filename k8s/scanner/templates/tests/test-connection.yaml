apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "scanner.fullname" . }}-test-connection"
  labels:
    {{- include "scanner.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "scanner.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
